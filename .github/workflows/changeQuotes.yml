name: Change qoute every day and push to GitHub
on:
  schedule:
    - cron: "0 */8 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update-quotes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install deps
        run: |
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          else
            npm install
          fi
      - name: Run index.js
        run: npm start
      - name: Commit & push quote changes
        run: |
          git config user.name "baiyuechuz"
          git config user.email "baiyuechuu@gmail.com"

          git add -A

          EMOJIS=("🌟" "🚀" "✨" "🎉" "💫" "🌈" "🔥" "⚡" "💎" "🌺" "🦋" "🍀" "🎨" "🎭" "🎪" "🎯" "🎲" "🎸" "🎵" "🎶" "📝" "📚" "💭" "💡" "🔮" "🌙" "☀️" "🌸" "🍃" "🌊")
          RANDOM_EMOJI=${EMOJIS[$RANDOM % ${#EMOJIS[@]}]}

          git commit -m "$RANDOM_EMOJI changes: Quotes" || exit 0
          git push origin HEAD:main
